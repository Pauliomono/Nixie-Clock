void setup() {
  // put your setup code here, to run once:
pinMode(13, OUTPUT);

pinMode(33, OUTPUT);
pinMode(34, OUTPUT);
pinMode(35, OUTPUT);
pinMode(36, OUTPUT);


digitalWrite(13, 1);
}

void loop() {
  // put your main code here, to run repeatedly:

}
